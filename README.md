# Build IQAS (Internal Quality Assurance System) Web App Using Laravel 9 Case Study For University in Indonesia

### Using NPM in docker
**To install dependencies:**
`docker-compose run --rm npm [CMD]`
